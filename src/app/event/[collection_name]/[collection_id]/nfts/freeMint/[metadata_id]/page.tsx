// Event freeMint page — reuses the collection freeMint component.
// Route params [collection_name] and [collection_id] are identically named
// so all useParams() calls inside the component resolve correctly.
export { default } from "@/app/collections/[collection_name]/[collection_id]/nfts/freeMint/[metadata_id]/page";
